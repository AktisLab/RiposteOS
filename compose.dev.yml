name: riposteos-dev

services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-riposteos}
      POSTGRES_USER: ${POSTGRES_USER:-riposteos}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-riposteos}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-riposteos} -d ${POSTGRES_DB:-riposteos}']
      interval: 2s
      timeout: 3s
      retries: 20

  migrate:
    image: mcr.microsoft.com/dotnet/sdk:10.0
    working_dir: /workspace
    command:
      - /bin/sh
      - -c
      - >-
        dotnet tool restore &&
        dotnet restore RiposteOS.slnx --artifacts-path /tmp/artifacts &&
        dotnet ef database update
        --project src/RiposteOS.Infrastructure
        --startup-project src/RiposteOS.Api
    environment:
      ArtifactsPath: /tmp/artifacts
      UseArtifactsOutput: 'true'
      ConnectionStrings__Database: Host=postgres;Port=5432;Database=${POSTGRES_DB:-riposteos};Username=${POSTGRES_USER:-riposteos};Password=${POSTGRES_PASSWORD:-riposteos}
    volumes:
      - .:/workspace
      - nuget_packages:/root/.nuget/packages
    depends_on:
      postgres:
        condition: service_healthy

  api:
    image: mcr.microsoft.com/dotnet/sdk:10.0
    init: true
    working_dir: /workspace/src/RiposteOS.Api
    command:
      - dotnet
      - watch
      - --non-interactive
      - --artifacts-path
      - /tmp/artifacts
      - run
      - --no-launch-profile
      - --urls
      - http://0.0.0.0:8080
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      DOTNET_USE_POLLING_FILE_WATCHER: '1'
      DOTNET_WATCH_SUPPRESS_LAUNCH_BROWSER: '1'
      ConnectionStrings__Database: Host=postgres;Port=5432;Database=${POSTGRES_DB:-riposteos};Username=${POSTGRES_USER:-riposteos};Password=${POSTGRES_PASSWORD:-riposteos}
    ports:
      - '${API_PORT:-8080}:8080'
    volumes:
      - .:/workspace
      - nuget_packages:/root/.nuget/packages
    depends_on:
      migrate:
        condition: service_completed_successfully

  worker:
    image: mcr.microsoft.com/dotnet/sdk:10.0
    init: true
    working_dir: /workspace/src/RiposteOS.Worker
    command:
      - dotnet
      - watch
      - --non-interactive
      - --artifacts-path
      - /tmp/artifacts
      - run
      - --no-launch-profile
    environment:
      DOTNET_ENVIRONMENT: Development
      DOTNET_USE_POLLING_FILE_WATCHER: '1'
      ConnectionStrings__Database: Host=postgres;Port=5432;Database=${POSTGRES_DB:-riposteos};Username=${POSTGRES_USER:-riposteos};Password=${POSTGRES_PASSWORD:-riposteos}
    volumes:
      - .:/workspace
      - nuget_packages:/root/.nuget/packages
    depends_on:
      migrate:
        condition: service_completed_successfully

  web:
    build:
      context: web
      dockerfile: Dockerfile.dev
    init: true
    command:
      - /bin/sh
      - -c
      - pnpm install --frozen-lockfile && exec pnpm dev
    environment:
      API_PROXY_TARGET: http://api:8080
      CI: 'true'
    ports:
      - '${WEB_PORT:-5173}:5173'
    volumes:
      - ./web:/app
      - web_node_modules:/app/node_modules
    depends_on:
      - api

  docling:
    image: quay.io/docling-project/docling-serve:v1.26.0
    profiles: ['ai']
    environment:
      DOCLING_SERVE_ENABLE_UI: '1'
    ports:
      - '${DOCLING_PORT:-5001}:5001'
    volumes:
      - docling_cache:/root/.cache

volumes:
  postgres_data:
  nuget_packages:
  web_node_modules:
  docling_cache:
